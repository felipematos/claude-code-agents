import React from 'react';
import Tasks from './Tasks';

/**
 * TaskBoard component - wrapper for the Tasks component
 * This provides a consistent naming convention for the task management interface
 */
function TaskBoard() {
  return <Tasks />;
}

export default TaskBoard;